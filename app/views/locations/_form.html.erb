<%= bootstrap_form_for(location) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :parent_location_name, disabled: true, value: location.parent_name %>
  </div>

  <div class="field">
    <%= form.hidden_field :location_id, id: :location_location_id %>
  </div>

  <div class="field">
    <%= form.text_field :name, id: :location_name %>
  </div>

  <div class="field">
    <%= form.text_field :description, id: :location_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
