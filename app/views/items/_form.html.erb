<%= bootstrap_form_for(item) do |form| %>
  <%# Render errors %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%# Render each field input %>
  <div class="field">
    <%= form.text_field :location, id: :item_location_name, disabled: true, value: item.location.name %>
  </div>

  <div class="field">
    <%= form.hidden_field :location_id, id: :item_location_id %>
  </div>

  <div class="field">
    <%= form.collection_select :stock_item_id, stock_items, :id, :name %>
  </div>

  <div class="field">
    <%= form.number_field :required, id: :item_required %>
  </div>

  <div class="field">
    <%= form.number_field :order_to, id: :item_order_to %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
